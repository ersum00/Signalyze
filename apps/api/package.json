{
  "name": "@signalyze/api",
  "version": "0.1.0",
  "private": true,
  "description": "pnpm bridge so `pnpm -r lint/test` also covers the Python API (requires uv).",
  "scripts": {
    "lint": "uv run ruff check . && uv run ruff format --check . && uv run mypy signalyze_api tests",
    "test": "uv run pytest -q"
  }
}
