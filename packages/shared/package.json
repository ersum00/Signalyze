{
  "name": "@signalyze/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared zod schemas, signal definitions and the forbidden-word list.",
  "exports": {
    ".": "./src/index.ts",
    "./forbidden-words.json": "./forbidden-words.json"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^5.0.0"
  }
}
